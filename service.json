{
    "kind": "Service",
    "metadata": {
        "labels": {
            "external": "true",
            "{{.NS_SELECTOR}}": "solution-webpack-ssh-{{.DEPLOY_HASH}}"
        },
        "name": "{{.SVC_NAME}}-{{.SVC_HASH}}"
    },
    "spec": {
        "selector": {
            "{{.NS_SELECTOR}}": "solution-webpack-ssh-{{.DEPLOY_HASH}}"
        },
        "ports": [
            {
                "protocol": "TCP",
                "targetPort": {{.PORT}}
            }
        ]
    }
}
